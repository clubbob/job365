---
description: 여러 줄 입력칸은 내용이 잘리지 않게 높이가 늘어나게 한다
alwaysApply: true
---

# 여러 줄 입력

사용자에게 여러 줄을 받는 칸은 기본 `textarea`를 쓰지 않는다. `AutoGrowTextarea`를 쓴다.

- 내용을 적으면 칸이 아래로 늘어나 입력한 줄이 모두 보여야 한다.
- 스크롤로 가리거나, 높이를 직접 늘리는 손잡이(`resize`)를 두지 않는다.
- 새로 만드는 폼에도 물어보지 말고 이렇게 적용한다.
